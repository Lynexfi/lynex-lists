// src/farms.ts
import { ChainId, Farm, Token } from '../types/index';
import { tokens } from './tokens';

export const farms: Farm[] = [
 /*  {
      pid: 0,
      lpSymbol: tokens.exampleLPToken.symbol, 
      stakeTokenAddress: tokens.exampleLPToken.address,
      token: tokens.exampleToken,
      quoteToken: tokens.anotherToken, 
      chainId: ChainId.LINEA_MAINNET,
      */
  ];